@using SchoolCloudMVC.BO
@model UsuarioBO
@{
    ViewBag.Title = "CambiarContraseña";
    Layout = "~/Views/Shared/MasterUsuarioAlumno.cshtml";
}
<div id="page-wrapper">
    <div class="container-fluid">
        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    Cambiar contraseña
                </h1>
                @*<ol class="breadcrumb">
                        <li>
                            <i class="fa fa-dashboard"></i>  <a href="index.html">Dashboard</a>
                        </li>
                        <li class="active">
                            <i class="fa fa-edit"></i> Forms
                        </li>
                    </ol>*@
            </div>
        </div>
        <!-- /.row -->
@using (Html.BeginForm("CambiarMiContraseña", "UsuarioAlumno", FormMethod.Post))
{
        <div class="row">
            <div class="col-lg-12">
                <div class="form-group">
                    <label>Contraseña</label>
                    @Html.TextBoxFor(x => x.confirmarcontraseña, null, new { @class = "form-control", @type = "Password" })
                   
                   
                </div>
                <div class="form-group">
                    <label>Confirmar contraseña</label>
                    @Html.TextBoxFor(x => x.Contraseña, null, new { @class = "form-control", @type = "Password" })
                   
                 
                </div>
                <div class="form-group">
                    <input type="submit"  class="btn btn-primary" value="Cambiar"/>
                </div>
            </div>
        </div>
}
            </div>
        </div>

<script language='javascript' type='text/javascript'>
		var dataexists = @ViewBag.DataExists;
		if (dataexists)
			alert('Data Already Exists');
</script>
